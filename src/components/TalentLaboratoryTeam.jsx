import React from "react";

export const TalentLaboratoryTeam = ({ teamMembers }) => {
  return (
    <div id="team-members">
      {teamMembers.map((teamMember) => (
        <></>
      ))}
    </div>
  );
};
